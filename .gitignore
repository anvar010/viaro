# v0 runtime files (should only exist in preview, not in git)
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Common ignores
node_modules/
.next/
.env*.local
.DS_Store
# TypeScript incremental build cache
*.tsbuildinfo

# Fontspring DEMO faces: evaluation-only, not licensed for distribution.
# Nothing renders from these — the site uses Archivo via next/font/google.
public/Demo_Fonts/
